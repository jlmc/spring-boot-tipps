package io.github.jlmc.doc4poc;

import org.junit.jupiter.api.Disabled;

@Disabled
//@TestConfiguration(proxyBeanMethods = false)
public class TestDoc4pocApplication {

    /*
    @Bean
    @ServiceConnection
    org.testcontainers.containers.PostgreSQLContainer<?> postgresContainer() {
        return new org.testcontainers.containers.PostgreSQLContainer<>(DockerImageName.parse("postgres:latest"));
    }

     */


    /*
    @Bean
    @ServiceConnection
    org.testcontainers.containers.RabbitMQContainer rabbitContainer() {
        return new org.testcontainers.containers.RabbitMQContainer(DockerImageName.parse("rabbitmq:latest"));
    }


    public static void main(String[] args) {
        SpringApplication.from(Doc4pocApplication::main).with(TestDoc4pocApplication.class).run(args);
    }
    */
}
